# Kopieer dit bestand lokaal naar .env en vul eigen waarden in.
# .env en .env.* worden door de repository genegeerd; commit nooit echte tokens.
MONEYBIRD_ADMIN_ID=replace-with-your-administration-id
MONEYBIRD_API_TOKEN=replace-with-your-local-token
